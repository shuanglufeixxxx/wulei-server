<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="wulei.mapper.FeaturedPostMapper">
    <select id="selectByClassify" resultType="Post">
        select post.id as id, pictureCollectionId, playbillId, title, essay, createDate, classify,
        previewPictureCollectionId, previewStyle
        from post join featuredPost on post.id = featuredPost.postId
        where classify = #{classify}
    </select>

    <insert id="insert" parameterType="FeaturedPost">
        insert into featuredPost (id, postId)
        values ( #{id}, #{postId} )
    </insert>
</mapper>