<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="wulei.mapper.AccountMapper">
    <select id="selectByUsername" resultType="Account">
        select * from account where username = #{username}
    </select>

    <select id="countByUsername" resultType="int">
        select count(*) from account where username = #{username}
    </select>

    <select id="countByUsernamePassword" resultType="int">
        select count(*) from account where username = #{param1} and password = #{param2}
    </select>

    <insert id="insert" parameterType="Account">
        insert into account ( id, username, password )
        values( #{id}, #{username}, #{password} )
    </insert>
</mapper>